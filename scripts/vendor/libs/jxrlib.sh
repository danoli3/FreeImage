VENDOR_DIR="Source/LibJXR"
SOURCE_SUBDIRS=""
ROOT_FILES=""
CMAKE_VAR=""
URL_TEMPLATE=""
# Microsoft's original jxrlib is dead/unmaintained (last real upstream
# activity ~2017); there is no canonical current release to bump to.
# Only community forks exist (e.g. github.com/cgohlke/jxrlib). Low
# priority - JPEG XR is a rarely-used format and switching source
# entirely would need its own evaluation, not a version bump.
