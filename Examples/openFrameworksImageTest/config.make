################################################################################
# CONFIGURE PROJECT MAKEFILE (optional)
################################################################################
# This file is left mostly default - see README.md in this folder for how to
# point an openFrameworks checkout's libs/FreeImage/ at this repo's build
# instead of oF's own bundled FreeImage before building this example.
